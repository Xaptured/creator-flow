/**
 * Request types for user preference endpoints.
 * Mirrors com.creatorflow.auth_service.dto.request.UpdateUserPreferencesRequest
 */

export interface UpdateUserPreferencesRequest {
  timezone: string
}
